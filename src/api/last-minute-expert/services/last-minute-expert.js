'use strict';

/**
 * last-minute-expert service
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::last-minute-expert.last-minute-expert');
