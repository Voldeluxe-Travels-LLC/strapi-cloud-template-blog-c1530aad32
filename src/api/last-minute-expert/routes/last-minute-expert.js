'use strict';

/**
 * last-minute-expert router
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::last-minute-expert.last-minute-expert');
