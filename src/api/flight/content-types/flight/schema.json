{
  "kind": "collectionType",
  "collectionName": "flights",
  "info": {
    "singularName": "flight",
    "pluralName": "flights",
    "displayName": "Flight"
  },
  "options": {
    "draftAndPublish": true
  },
  "pluginOptions": {},
  "attributes": {
    "TripType": {
      "type": "enumeration",
      "required": true,
      "enum": [
        "One-way",
        "Round-trip"
      ]
    },
    "Price": {
      "type": "text",
      "required": true
    },
    "Cabin": {
      "type": "enumeration",
      "enum": [
        "Economy",
        "Premium Economy",
        "Business",
        "First"
      ]
    },
    "AirlineCard": {
      "type": "component",
      "component": "fields.flight-card",
      "repeatable": true,
      "required": true
    }
  }
}
